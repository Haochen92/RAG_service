# Notebooks

This folder is for experiments and scratch work before moving code into `src/`.

Recommended setup:
```bash
poetry install --with notebook
poetry run python -m ipykernel install --user --name rag-service
poetry run jupyter lab
```

