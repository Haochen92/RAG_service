## Postgres (used by `docker-compose.yml` and the app defaults)
POSTGRES_USER=rag
POSTGRES_PASSWORD=rag
POSTGRES_DB=rag
POSTGRES_HOST=localhost
# Host port exposed by Docker (container listens on 5432)
POSTGRES_PORT=6006

## Optional: set a full URL instead of POSTGRES_*
# DATABASE_URL=postgresql+asyncpg://rag:rag@localhost:6006/rag

## pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
PGADMIN_PORT=5051

## Notebooks / embeddings
# GEMINI_API_KEY=...
